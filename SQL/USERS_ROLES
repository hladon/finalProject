CREATE TABLE USERS_ROLES
(
  ID  NUMBER PRIMARY KEY,
  USERS NUMBER,
  CONSTRAINT USER_FORK FOREIGN KEY (USERS)
  REFERENCES USER_PROFILE (ID),
  ROLE  NUMBER,
  CONSTRAINT ROLE_FK FOREIGN KEY (ROLE)
  REFERENCES ROLE (ID)
);